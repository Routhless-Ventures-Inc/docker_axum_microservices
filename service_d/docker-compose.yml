version: '3.8'

services:
  service_d:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: service_d
    ports:
      - "3000:3000"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
